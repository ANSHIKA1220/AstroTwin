NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Production only: optional server-side override for the same-origin /api proxy.
# If omitted, next.config.ts uses NEXT_PUBLIC_API_URL as the proxy destination.
API_PROXY_TARGET=
